name: Deploy CloudFormation Stack

on:
  push:
    branches:
      - main
    paths:
      - 'infra/**'
      - '.github/workflows/deploy.yaml'
    # Allowed environment values: ci, devl, test, prod
    # To deploy to a different environment, update the hardcoded value in the job

jobs:
  deploy:
    uses: subhamay-bhattacharyya-gha/cloudformation-deploy-wf/.github/workflows/cfn-deploy.yaml@feature/GHA-0003-initial-release-cloudform
    with:
      environment: devl
      aws-region: ${{ vars[format('AWS_REGION_{0}', 'devl')] || vars.AWS_REGION }}
      aws-account-id: ${{ vars[format('AWS_ACCOUNT_ID_{0}', 'devl')] || vars.AWS_ACCOUNT_ID }}
      oidc-role-name: ${{ vars[format('OIDC_ROLE_NAME_{0}', 'devl')] || vars.OIDC_ROLE_NAME }}
      cfn-templates-s3-bucket: ${{ vars[format('CFN_TEMPLATES_S3_BUCKET_{0}', 'devl')] || vars.CFN_TEMPLATES_S3_BUCKET }}
      stack-name: ${{ github.event.repository.name }}
      template-file: infra/template.yaml
      parameters-file: infra/parameters.json
