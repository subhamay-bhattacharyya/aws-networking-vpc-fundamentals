AWSTemplateFormatVersion: '2010-09-09'
Description: 'Basic CloudFormation template to create an S3 bucket'

Resources:
  S3Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub 'subhamay-test-bucket-${AWS::AccountId}-${AWS::Region}'
